
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:tn}
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false
server:
  port: 8111
spring:
  application:
    name: userservice
  jpa:
    hibernate:
      ddl-auto:
        create
  datasource:
    url:
      jdbc:mysql://localhost:3306/microservices_student
    username:
      root
    password:
      Massaq51354
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
    enabled: true
