
server:
  port: 8111
spring:
  application:
    name: user-service
  jpa:
    hibernate:
      ddl-auto:
        create
  datasource:
    url:
      jdbc:h2:mem:user-service
    username:
      root
    password:
      12345

  driver-class-name: org.h2.Driver
  h2:
    console:
      enabled:true




eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
    enabled: true



